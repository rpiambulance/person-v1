kind: pipeline
name: deploy_person-v1

steps:
  - name: deploy
    image: appleboy/drone-ssh
    settings:
      host: lp12.rpiambulance.com
      username: drone
      password:
        from_secret: SSH_PASSWORD
      port: 22
      script:
        - docker-compose -f /drone/docker-compose.yml up -d person-v1
